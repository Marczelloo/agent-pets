import type { Pet } from '../renderer/pet';
import type { MotionDef } from './types';

/** Efekty Anime rysowane poza ciałem (task 9). */
export function drawFx(_x: CanvasRenderingContext2D, _c: Pet, _X: number, _Y: number, _u: number, _t: number, _m: MotionDef, _energy: number): void {}
