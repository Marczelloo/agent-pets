{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Scena, tooltip i panel",
  "windows": ["stage*", "tooltip", "panel"],
  "permissions": ["core:default"]
}
